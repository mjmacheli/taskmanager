import { ApplicationAuditedEntity } from './application-audited-entity';

describe('ApplicationAuditedEntity', () => {
  it('should create an instance', () => {
    expect(new ApplicationAuditedEntity()).toBeTruthy();
  });
});
